# Contributing

변경은 Issue → branch → pull request → CI → merge로 기록합니다.
`npm ci --ignore-scripts` 후 `npm run check`로 공개 범위와 자동 검사를 확인합니다.
실제 GPU 학습, 원본 영상, 실서비스 배포는 CI에 포함하지 않습니다.

최초 일곱 단계는 보존된 개발 결과를 현재 날짜에 공개용으로 정리한 것입니다.
AI 보조 구현과 자체 검토를 사용했으며 독립적인 사람의 승인으로 표시하지 않습니다.
검사 통과는 실제 모델의 시각적 품질이나 실기기 성능을 보증하지 않습니다.

개인 영상·원본 로그·인증정보를 이슈나 PR에 첨부하지 않습니다.
